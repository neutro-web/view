# Slot Increment 1.5 — Emit-path thunk-assembly collapse (E-2b) + component-in-conditional-branch fix

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Collapse the emit-path's thunk-assembly into a single recursive builder, fixing the component-in-conditional-branch emit bug and deleting confirmed-dead emitter cases.

**Architecture:** Extract `computeBindingThunks` from `parseNvFileForEmit`'s inline assembly; route the conditional branch recursion through it using the branch's existing `ProcessResult`; delete `emitThunkSource`'s dead structural cases. Three internal steps (A: behavior-neutral collapse, B: fix falls out, C: dead-code deletion) but one CC session / one task.

**Tech Stack:** TypeScript, vitest, biome. `nv-parser.ts` + `nv-emitter.ts` only.

## Global Constraints

- `src/core/` UNTOUCHED — reactive-core v0.4.2 unchanged
- `ir.ts` UNTOUCHED — Template-IR v0.3.3 unchanged
- `interpreter.ts`, `emitted-mount.ts`, `html-tag.ts` UNTOUCHED
- Production changes confined to `src/renderer/nv-parser.ts` + `src/renderer/nv-emitter.ts`
- No IR shape change; no contract change
- This is NOT increment 2 — no `SlotEntry.content` factory, no `SlotOutletBinding.props`, no `let={}`, no D-slot-2
- Step A behavior-neutrality: existing emit corpus (EM-*, EX-*, TC-C*, EM-D1c) green, zero assertion edits
- Step C: three `emitThunkSource` cases (`conditional`/`component`/`slot-outlet`) confirmed dead by Q3 spike (3239 green, zero REACHED) — delete only after re-running the probe with Step B tests included

---

### Task 1: E-2b collapse + fix + dead-code deletion

**Full brief:** `docs/superpowers/plans/slot-inc1.5-handoff.md` — read it in full before writing any code. It is the authoritative requirements document including the escalation rule (§3), which is load-bearing.

**Files:**
- Modify: `src/renderer/nv-parser.ts` — extract `computeBindingThunks`; wire conditional recursion through it
- Modify: `src/renderer/nv-emitter.ts` — delete dead `emitThunkSource` cases; narrow type
- Modify/create: `test/renderer/nv-emitter.test.ts` or `test/renderer/slot-consumption.test.ts` — Step B fail-shows-teeth test

**Interfaces:**
- Consumes: `ProcessResult` from `processHtmlTemplate` (already produced by `buildNvHoleBinding`'s conditional case — thread it, don't re-walk)
- Produces: `computeBindingThunks(procResult, holeExprs, positions, doc, signals, diagnostics, propsParamName?, propsAccessors?): ThunkSource[]` — replaces the inline assembly in `parseNvFileForEmit`

**Step A — behavior-neutral collapse:**

- [ ] Read `parseNvFileForEmit`'s `bindingThunks` assembly block, `computeThunksForTemplate`, `computeThunkSource`'s conditional case, and `buildNvHoleBinding`'s conditional case. Map the exact data flow.
- [ ] Extract `computeBindingThunks` from the inline assembly. `parseNvFileForEmit` calls it for the top-level render result. Adjust `computeThunkSource`'s conditional case to call `computeBindingThunks` with the branch's `ProcessResult` (from `buildNvHoleBinding`'s existing `processHtmlTemplate` call — thread it; do not re-walk).
- [ ] **ESCALATION CHECK:** If threading the `ProcessResult` requires a signature change whose cleanliness is uncertain, or would require re-walking the branch — STOP. Surface to architect. Do not fall back to local component detection in the conditional case (that is E-2a).
- [ ] Run `npx vitest run test/renderer/nv-emitter.test.ts test/renderer/nv-emitter-exec.test.ts` — all existing EM-*, EX-*, TC-C*, EM-D1c must pass with ZERO assertion edits. Report count delta = 0. If any assertion changes, STOP.
- [ ] Commit Step A alone: `refactor(nv-parser): extract computeBindingThunks — E-2b emit-path collapse (behavior-neutral)`

**Step B — Q1 fix falls out:**

- [ ] Run the Q1 spike inline to confirm the fix: `parseNvFileForEmit` on the Toggle source (see §4 of brief) → `bindingThunks[0].consequent[0].kind === 'component'`; `emitModule` does not throw.
- [ ] Add the fail-shows-teeth test to `test/renderer/nv-emitter.test.ts` (or a new `§inc1.5` describe block in `slot-consumption.test.ts`):
  - Assert `bindingThunks[0].consequent[0].kind === 'component'` and `componentSrc === 'Card'`
  - Assert `ir.bindings[0].consequent.bindings[0].kind === 'component'` (alignment)
  - Assert `emitModule(results)` does not throw and emitted JS contains `kind: 'component'` in the conditional consequent
  - **Fail-shows-teeth procedure:** Revert to hole-only branch path (comment out `computeBindingThunks` call in conditional case, restore `computeThunksForTemplate`). Test MUST FAIL with `ComponentBinding thunk kind mismatch`. Restore. Test MUST PASS. Report both; do not commit the reverted state.
- [ ] Add component-in-slot-in-conditional nesting test if it falls out trivially from the same seam. If not, note as follow-on.
- [ ] Anti-vacuous sweep: `grep -rPzo "expect\(\s*(true|false)\s*\)\.toBe"` + `grep -rPzo "expect\(\s*!"` over new tests → zero matches.
- [ ] Run full suite: `npx vitest run`. Report count (expected: 3239 + new tests).
- [ ] Commit: `fix(nv-parser): component-in-conditional-branch emit — component ThunkSource aligned via E-2b collapse`

**Step C — dead-code deletion:**

- [ ] Add `throw new Error('REACHED-conditional')`, `throw new Error('REACHED-component')`, `throw new Error('REACHED-slot-outlet')` as first lines of the respective `emitThunkSource` cases. Run full suite. Report zero REACHED. If any REACHED fires, STOP — surface to architect.
- [ ] Delete the three cases. `emitThunkSource` retains only `text`/`attr`/`prop`/`event`. Update the `ThunkSource` type parameter / switch exhaustiveness so `tsc --strict` proves the three kinds can no longer reach it.
- [ ] Run `npx tsc -p tsconfig.json --noEmit` — clean.
- [ ] Run `npx biome check src/ test/` — clean.
- [ ] Run full suite — green.
- [ ] Commit: `refactor(nv-emitter): delete dead emitThunkSource conditional/component/slot-outlet cases (Q3-confirmed)`

**Step D — on-land docs:**

- [ ] Append LANDED entry to `docs/decision-log.md`: E-2b collapse landed; component-in-conditional-branch fixed; dead cases deleted. Cite *Slot increment 1 LANDED [2026-06-22]* and GATE-2 as structural precedent. No contract/IR version change.
- [ ] Update Current State in `docs/decision-log.md`: increment 1.5 landed; increment 2 queued.
- [ ] Update `docs/implementation-state.md`: `nv-parser.ts` emit path now single recursive thunk-builder; `nv-emitter.ts` `emitThunkSource` leaf-only.
- [ ] Commit: `docs: increment 1.5 landed — emit-path collapse + conditional-branch component fix`
- [ ] `git push`

**Gates (report each before push):**
- `tsc --strict` clean
- Full vitest green; report count delta vs 3239
- biome clean
- Step A behavior-neutrality: existing emit corpus zero assertion edits
- Step B fail-shows-teeth pair reported
- Step C reachability probe (zero REACHED) reported before deletion
- Scope proof: `git show --stat` production changes confined to `nv-parser.ts` + `nv-emitter.ts` + tests
