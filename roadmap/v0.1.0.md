# Checkpoint v0.1.0 — Minimum Viable View Engine

> **Meaning:** nv can be authored as a real app end-to-end **in a real browser**, and dropped in as
> the core under js-framework-benchmark (keyed) for apples-to-apples performance measurement.
> **Bar:** "works end-to-end + benchmarkable" — NOT feature-complete.

**Status:** IN PROGRESS. **Reconciled to** `007eaa2` (2026-06-25).

The engine internals are largely complete and well-tested in jsdom. The gap to v0.1.0 is the
**integration + real-browser-truth layer**, not core machinery.

---

## Critical path (must all be DONE to reach v0.1.0)

### CP-1 — Real-browser end-to-end authoring path
**Status: CLOSED — path PROVEN E2E + production-viable bundle.**
The `.nv` authoring probe (`8146d82`) proved the full path works end-to-end in real browsers:
`counter.nv` → `nvPlugin` → esbuild → IIFE bundle → chromium + webkit, click increments DOM text.
Authoring is assignment-form (`@click="${() => count = count + 1}"`), parser-erased to
`count.set(count() + 1)` at build time; bare `${count}` → `count()`. Authors write neither.

- **CP-1a — `.nv` → esbuild plugin → bundle → mount → interact, in a real browser. CLOSED `0b49444`.**
  Path proven (probe chromium+webkit). Production-viability blocker resolved: runtime entry
  `@neutro/view/renderer/runtime` exports `mount` only (from `./interpreter.js`); emitter targets it
  (`nv-emitter.ts` L318), so emitted bundles no longer pull the TS compiler via the fat barrel.
  **Emitted counter bundle: 9.9 MB → 34.9 KB unminified, zero `typescript` inputs.** Regression guard
  `runtime-bundle-gate.test.ts` (G1 runtime standalone + G2 emitted bundle) fails closed on TS-compiler
  re-entry, asserted against the real esbuild metafile. Fat barrel + `./renderer` entry untouched.
  (decision-log 2026-06-25 runtime/build-time split RULED.)
- **CP-1b — event-handler → signal-write authoring works in `.nv`.** **CLOSED `8146d82`** —
  "documented pattern, not missing wiring." Assignment-form handler parses, emits, wires, and
  updates the DOM on click in real browsers. No wiring gap.

### CP-2 — js-framework-benchmark keyed implementation
**Status: NOT STARTED (one OPEN QUESTION inside).**
A `frameworks/keyed/nv/` implementation that builds via `npm run build-prod` and passes the
webdriver checks.

- **CP-2a — author the benchmark app in `.nv`** (table, action-trigger buttons with exact ids,
  Bootstrap CSS, row select/swap/remove). Exercises `list` (keyed), `event`, `classlist`
  (selected-row toggle). **NOT STARTED.**
- **CP-2b — keyed-swap compliance (VERIFY, reopen NOT expected).** The benchmark's keyed-swap rule
  requires *moving* the actual DOM rows, not remove+reinsert (else DOM state is lost). **Source read
  (`interpreter.ts` L534–544): the reconcile already moves existing `rootEl` nodes via
  `insertBefore` in reverse** — and `insertBefore` on a node already in the document *moves* it
  (preserves element identity and DOM state). The 2026-06-22 LIS closure also records single-move as
  O(1) DOM mutations. So nv **likely already satisfies `isKeyed` for swap by construction.** Action
  is to **verify** `npm run isKeyed keyed/nv` passes once CP-2a exists — *not* to reopen LIS. LIS
  reopen would only be triggered by a *measured* swap-throughput deficit at benchmark scale, a
  separate and currently-unsupported hypothesis. **Do not pre-commission an LIS reopen.** → LIS
  closure: decision-log 2026-06-22.
- **CP-2c — record baseline numbers** vs Solid/Svelte/vanilla on the standard ops (create/replace/
  update/select/swap/remove/create-10k/append/clear + memory). Real-hardware, CC-run. **NOT
  STARTED.**

### CP-3 — Public API is authorable without IR knowledge
**Status: CLOSED `8146d82`.**
Probe confirmed no thin wrapper is needed. The emitted module already exposes
`Component.mount(parent, doc)` as a static method; the documented convention is
`import { Counter } from './app.nv'; Counter.mount(document.body, document)`. App authors never touch
raw IR. (Author-facing docs for this convention are part of CP-4.)

### CP-4 — Minimal documentation for the above
**Status: NOT STARTED.**
Enough to let someone author a `.nv` app: primitives, the four authoring macros (`$component`,
`$render`, `$style`, props), control flow (`<each>`, conditional), events, `sync`. Not full docs —
the floor that makes the benchmark app and a small clone writable by someone who isn't the architect.

---

## Already DONE (the engine floor — verified)

- **Reactive core** — `signal`, `derived`, `effect`, `sync`, `pubsub`, `errorBoundary`, `batch`,
  `untrack`, `createRoot`, `onCleanup`, `flushSync`. DOM-free, confirmed 2026-06-24. Contract v0.4.2.
- **IR binding vocabulary** — text, attr, prop, event, child, conditional, list (keyed), sync,
  component, slot-outlet, classlist, style-var. Sufficient for a HN/Twitter-class app's rendering.
- **Both front-ends** — `.nv` parser + tagged-template, FE-equivalence-gated, one shared IR.
- **Both back-ends** — interpreter + compiler/emitter (`emitMount`), differential-gated.
- **esbuild plugin** — `.nv` loader exists (untested e2e — see CP-1a).
- **Slot domain** — CLOSED `58afe25`; `<each>`-in-slot both FEs, scoped slots, `$style` scoping.
- **Wide-graph propagation** — EVIDENCE-TESTED CLEAR `387990f`; ~0.15–0.17ms/tick, ~100× under
  frame budget. Propagation is not a top user-facing cost at realistic scale.

---

## Deferred (does NOT block v0.1.0; trigger noted)

- **G-SS-browser Playwright ×3** (`$style × <each>-in-slot` styled cascade) — element-targeting
  jsdom-proven; real-cascade CSS-application waits on a browser-harness trip. *Trigger:* next
  Playwright run. Folds naturally into CP-1's real-browser work. → decision-log 2026-06-23 bookkeeping.
- **kind-split** — accepted structural gap, evidence-tested CLEAR. *Trigger:* real-app graph
  breaching frame budget in real profiling. → decision-log 2026-06-25.
- **checkProgram build-wiring** — callable, not auto-wired. *Trigger:* a production flow constructs
  a `ts.Program` over user source. → Current State.

---

## OPEN QUESTIONS (need architect rulings to scope)

1. **Multi-root list items** — single-root guard today; close before promoting multi-root. Does the
   benchmark table need multi-root rows? (Likely no — `<tr>` is single-root, so not a v0.1.0 blocker.)

*(CP-3 author-entry question — RESOLVED by probe `8146d82`: emitted `Component.mount(parent, doc)`
static method is the convention, no wrapper needed. CP-2b keyed-swap-forces-LIS question — resolved
by source read; reconcile already moves nodes. Both downgraded out of open questions.)*

---

## Status log
- **2026-06-25** — Checkpoint created. Engine floor confirmed DONE at `007eaa2`; integration +
  real-browser layer (CP-1..4) identified as the gap. `.nv` authoring probe commissioned to Sonnet/CC.
- **2026-06-25** — Probe `8146d82` landed. CP-1b + CP-3 CLOSED (author path proven E2E in real
  browsers; static `.mount` convention; no wiring gap, no wrapper needed). CP-1a path proven but
  gains a production-viability blocker: emitted bundles pull ~4 MB TS compiler via the fat renderer
  barrel — runtime-entry split ruled + commissioned (`cc-handoff-runtime-package-split.md`).
- **2026-06-26** — CP-1a CLOSED at `0b49444`. Runtime/build-time package split landed:
  `src/renderer/runtime.ts` (`mount`-only), `package.json` `./renderer/runtime` export, emitter
  retargeted, `runtime-bundle-gate.test.ts` added. Emitted bundle 9.9 MB → 34.9 KB, zero TS-compiler
  inputs; gate verified biting on source read. Runtime surface confirmed `mount`-only — the
  speculative `createHtmlTag`/`structurallyEqual` runtime exports were not needed (emitter emits two
  import lines: core primitives + `mount`). **CP-1 fully CLOSED.** Next: CP-2a (benchmark app).
